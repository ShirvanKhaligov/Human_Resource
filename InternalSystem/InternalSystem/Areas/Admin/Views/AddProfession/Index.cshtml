@model IEnumerable<Profession>
@{
    ViewData["Title"] = "Index";
}

<div class="content-wrapper" style="padding:0px 0px 0px 250px">
    <div class="container-fluid">
        <br>
        <div class="card">
            <div class="card-header">
                <p style="text-align: center;font-size: 20px;color: green">İxtisasların siyahısı</p>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-12">
                        <a asp-area="Admin" asp-controller="AddProfession" asp-action="Create" class="btn btn-success ">Yeni ixtisas <i class=" fas fa-plus"></i></a>
                    </div>
                </div>
                <br><br>
                <div class="row">
                    <div class="col-md-12" id="professionTableDiv">
                        <table class="table table-hover table-responsive-md ">
                            <thead style="text-align: center" class="thead-dark ">
                                <tr>
                                    <th scope="col"> Sıra№</th>
                                    <th scope="col">İxtisasın adı</th>
                                    <th scope="col">Fakultənin adı</th>
                                    <th scope="col">Universitetin adı</th>
                                </tr>
                            </thead>
                            <tbody style="text-align: center;color:blue;">
                                @if (Model.Count() != 0)
                                {
                                    var count = 1;

                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <th scope="row">@count</th>
                                            <td>@item.Name</td>
                                            <td>@item.UniFaculty.Name</td>
                                            <td>@item.UniFaculty.EducationTypeToUniversity.Universities.Name (@item.UniFaculty.EducationTypeToUniversity.EducationType.Name)</td>
                                        </tr>
                                        count++;
                                    }
                                }
                                else
                                {
                                    <tr class="text-danger text-bold">
                                        <th scope="row">0</th>
                                        <td>Boş</td>
                                        <td>Boş</td>
                                        <td>Boş</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            @if (Model.Count() != 0)
            {
                <div class="card-footer">
                    <a asp-area="Admin" asp-controller="AddProfession" asp-action="AllProfessions" class="btn btn-primary col-md-12">Bütün ixtisasların siyahısı</a>
                </div>}

        </div>

    </div>

    <!--content-wrapper-->
</div>

