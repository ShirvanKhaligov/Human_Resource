@model IEnumerable<DevicesType>
@{
    ViewData["Title"] = "Index";
}

<div class="content-wrapper" style="padding:0px 0px 0px 250px">
    <div class="container-fluid">
        <br>
        <div class="card">
            <div class="card-header">
                <p style="text-align: center;font-size: 20px; color: green">Qurğuların siyahısı</p>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-12">
                        <a asp-area="Admin" asp-controller="AddDevice" asp-action="Create" class="btn btn-success">Yeni qurğu əlavə et <i class=" fas fa-plus"></i></a>
                    </div>
                </div>
                <br><br>
                <div class="row">
                    <div class="col-md-12 mx-auto" id="departmentTableDiv">
                        <table style="color:blue" class="table table-hover table-responsive-md ">
                            <thead style="text-align: center" class="thead-dark ">
                                <tr>
                                    <th scope="col">Sıra №</th>
                                    <th scope="col">Qurğunun adı</th>
                                </tr>
                            </thead>
                            <tbody style="text-align: center">
                                @{ var count = 1; }
                                @if (Model.Count() != 0)
                                {

                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <th scope="row">@count</th>
                                            <td>@item.Name</td>
                                        </tr>
                                        count++;
                                    } }
                                else
                                {
                                    <tr>
                                        <th scope="row">0</th>
                                        <td>Siyahı boşdur</td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>


                </div>


            </div>
            @if (Model.Count() != 0)
            {
                <div class="card-footer">
                    <a asp-area="Admin" asp-controller="AddDevice" asp-action="AllDevice" class="btn btn-primary col-md-12">Bütün qurğuların siyahısı</a>
                </div>}

        </div>
    </div>
</div>
